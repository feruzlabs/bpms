# Compat corpus (plan 14 Phase 1)

## credit-conveyor/

Snapshots of repo-root `processes/*.bpmn`. Used by `CompatCorpusParseTest`.

Expected: **warnings=0** for all files after Phase 2 parser covers old dialect.

## external/

Other teams' schemas (hrms/labour) — **empty until provided** (Phase 0 blocker §4.1(2)).

Drop 3–5 representative `.bpmn` files here when available.