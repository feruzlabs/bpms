# Kontekst — 2026-07-22 sessiyasi natijalari

> Bu hujjat shu kungi ish natijalarini (rejalar, qarorlar, topilmalar) yangi sessiya uchun jamlaydi.
> To'liq indeks: `docs/plans/README.md`. Umumiy kontekst: `docs/context/00-PROJECT-CONTEXT.md`.

## Yaratilgan rejalar (bugun)
| # | Reja | Mavzu |
|---|---|---|
| 22 | gateway unconditional-flow fix | merge/shartsiz flow implicit default |
| 23 | execution_token_state + listener trace | history→state, BEFORE/AFTER listener |
| 24 | Flyway→Liquibase | amalda **XML** changelog |
| 25 | schema v3 DDL | ~29 jadval, FK/CHECK/partition/trigger/seed |
| 26 | FEATURES.md | A–L biznes-feature hujjati |
| 27 | instance TERMINATE + runaway guard | step-budjet, node-revisit, cooperative cancel |
| 28 | timer/message/signal | **P0 timer** (intermediate catch, `timeDuration`) — kredit polling |
| 29 | DoD gap-close | 22/25/27 test bo'shliqlari |
| 30 | start-form + business key | 10/10 korpus start-forma |
| 31 | human-workflow | userTask/manualTask/terminateEnd/initiator |
| 32 | qolgan 7 element | boundary/throw/callActivity/subProcess/receive/send/businessRule |
| 33 | god-class refactor | ExecutionEngine→NodeBehavior, JpaPersistenceAdapter→port |
| 34 | public API test | http-json-get connector + valyuta kursi bpmn |
| 35 | process versioning | checksum-based versiya aniqlash + versiya qayerda kerak |

Qo'shimcha: **ADR-001** (`docs/adr/`) — skript-listener + `bpms.*` vs service-task; **best-practice namuna**
(`docs/examples/regional_vacation_bestpractice.bpmn`).

## Muhim topilmalar (kod bo'yicha)
### Engine element realizatsiyasi — 9/17 ijro qilinadi
- **Ijro qilinadi (9):** startEvent(+start-form), endEvent(+terminateEnd), serviceTask(connector), scriptTask,
  userTask, manualTask, exclusiveGateway, inclusiveGateway, parallelGateway.
- **Model bor, ijro YO'Q (8):** `intermediateCatchEvent` (**timer — reja 28, kredit blocker**), intermediateThrow,
  boundaryEvent, callActivity, subProcess, receiveTask, sendTask, businessRuleTask.
- **Verdikt:** HR/approval sinfi (regional_vacation) ishlaydi; **kredit korpus timer'siz to'liq end-to-end EMAS** —
  yagona real blocker = reja 28.

### God-class'lar (refactor nomzodlari — reja 33)
- `ExecutionEngine.java` — **793 qator** (barcha node dispatch + gateway + lifecycle).
- `JpaPersistenceAdapter.java` — **685 qator** (barcha persistence port bitta klassда).
- Ikkinchi: `ElementMapper` (315), `ProcessEngineService` (253).
- **Tavsiya:** reja 33 (NodeBehavior refactor) ni reja 32 (7 element)дан **oldин** — aks holда god-class 1200+ qatorга o'sади.

### Versiyalash (reja 35)
- Hozir: `nextVersion = max(version)+1` → **har deploy = yangi versiya** (kontent bir xил bo'lса ham); `bpmn_checksum`
  ustuni **ishlatilmayapti** → "yangi versiyami" bilib bo'lmayapti.
- `process_definition` **allaqачон versiya jadvали** (har qator = versiya). Yechim: checksum-dedup + deploy javobида
  `changed` + `process_instance`ga denorm `definition_version` + katalog endpoint.

### BISetStateConnector
- ✅ Ko'chirilган va `CreditConveyerConnectorProvider`да ro'yxатда (connector #40). `BISetStateCommonConnector`
  ko'chирилмаган, lekin hech qайси sxема ishlатмайди — kerak emas.

## Korpus grounding (real sxемалар nima ishlатади)
- **Timer** (`intermediateCatchEvent` + `timeDuration` PT1M/PT30S): **10/10 kredit sxемада** (polling loop).
- **Start-forma:** 10/10.
- **Yo'q:** message/signal/boundary/callActivity/subProcess/multiInstance/businessRuleTask/userTask (kredit).
- **regional_vacation (HR):** 7 userTask + 13 manualTask + 8 gateway + 6 terminateEnd + 56 skript-listener (`bpms.*`).

## Holat / keyingi
- **Implement navbati (Cursor):** 28 (⚠️ timer — kredit blocker), 29, 32, 33, 34, 35.
- **Tavsiya tartib:** 33 (refactor) → 28 (timer) → 32 (elementlar) → 30/31 tasdiq → 27 guard → 35 (versioning).
- **Hali reja qilinmagan (kelajak):** Trigger/Scheduler qatlami (timer-cron start + webhook — n8n yo'nalishi),
  generic connector katalog, outbox poller, request-scoped multi-tenancy, shadow-run parity, catalog/validate API.
- **Muhim eslatма:** reja backlog chuqur — asosий "qolган" ish endi **implementatsiya**, reja emas.

## Ishlash uslubi (eslatма)
Rejalar `docs/plans/`da; men (Claude/Cowork) **faqat plan yozaman, kod emas** — Cursor/Claude Code bajaради.
Har reja self-contained + DoD + "eski bpms 0 diff".
